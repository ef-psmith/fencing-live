# Never forward addresses in the non-routed address spaces.
bogus-priv

interface=eth0
addn-hosts=/home/engarde/public/hosts

domain=fencingresults.net

dhcp-range=192.168.2.50,192.168.2.150,12h

# Specify a subnet which can't be used for dynamic address allocation,
# is available for hosts with matching --dhcp-host lines. Note that
# dhcp-host declarations will be ignored unless there is a dhcp-range
# of some type for the subnet in question.
# In this case the netmask is implied (it comes from the network
# configuration on the machine running dnsmasq) it is possible to give
# an explicit netmask instead.
#dhcp-range=192.168.0.0,static

# Always allocate the host with Ethernet address 11:22:33:44:55:66
# The IP address 192.168.0.60
#dhcp-host=11:22:33:44:55:66,192.168.0.60

# Always set the name of the host with hardware address
# 11:22:33:44:55:66 to be "fred"
#dhcp-host=11:22:33:44:55:66,fred

# Always give the host with Ethernet address 11:22:33:44:55:66
# the name fred and IP address 192.168.0.60 and lease time 45 minutes
#dhcp-host=11:22:33:44:55:66,fred,192.168.0.60,45m

# Give a host with Ethernet address 11:22:33:44:55:66 or
# 12:34:56:78:90:12 the IP address 192.168.0.60. Dnsmasq will assume
# that these two Ethernet interfaces will never be in use at the same
# time, and give the IP address to the second, even if it is already
# in use by the first. Useful for laptops with wired and wireless
# addresses.
#dhcp-host=11:22:33:44:55:66,12:34:56:78:90:12,192.168.0.60

# Give the machine which says its name is "bert" IP address
# 192.168.0.70 and an infinite lease
#dhcp-host=bert,192.168.0.70,infinite

dhcp-option=3,192.168.2.1

# The following DHCP options set up dnsmasq in the same way as is specified
# for the ISC dhcpcd in
# http://www.samba.org/samba/ftp/docs/textdocs/DHCP-Server-Configuration.txt
# adapted for a typical dnsmasq installation where the host running
# dnsmasq is also the host running samba.
# you may want to uncomment some or all of them if you use
# Windows clients and Samba.
dhcp-option=19,0           # option ip-forwarding off
dhcp-option=44,0.0.0.0     # set netbios-over-TCP/IP nameserver(s) aka WINS server(s)
dhcp-option=45,0.0.0.0     # netbios datagram distribution server
dhcp-option=46,8           # netbios node type

# Send an empty WPAD option. This may be REQUIRED to get windows 7 to behave.
#dhcp-option=252,"\n"

dhcp-leasefile=/home/engarde/public/dnsmasq.leases

dhcp-host=sip01,192.168.2.151,infinite
dhcp-host=sip02,192.168.2.152,infinite
dhcp-host=sip03,192.168.2.153,infinite
dhcp-host=sip04,192.168.2.154,infinite
dhcp-host=sip05,192.168.2.155,infinite
dhcp-host=sip06,192.168.2.156,infinite
dhcp-host=sip07,192.168.2.157,infinite
dhcp-host=sip08,192.168.2.158,infinite
dhcp-host=sip09,192.168.2.159,infinite
dhcp-host=sip10,192.168.2.160,infinite
dhcp-host=sip54,192.168.2.174,infinite
dhcp-host=sip55,192.168.2.175,infinite
